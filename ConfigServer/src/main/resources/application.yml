Spring:
  application:
    name: "ConfigServer"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-location: "classpath:/config"

server: 
  port: 8071
  